# Inkova uses Ollama for free local AI — no API key needed.
# Make sure Ollama is running: ollama serve
# Pull the model first: ollama pull mistral

# Optional: override the default model
VITE_OLLAMA_MODEL=mistral
